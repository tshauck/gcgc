jobs:
  include:
  - install:
    - pip install poetry
    - poetry install
    script: make test
    language: python
    dist: xenial
    sudo: required
    python:
    - '3.7'
  - install:
    - pip install poetry
    - poetry install
    script: make docs docs_upload
    language: python
    dist: xenial
    sudo: required
    python:
    - '3.7'
