[build-system]
requires = ["setuptools>=40.8", "wheel", "setuptools_scm>=1.15"]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 100
include = '\.pyi?$'
exclude = '''
/(
   #\.git
 #| \.mypy_cache
 #| \.tox
 #| build
 #| dist
)/
'''
